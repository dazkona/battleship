//-------------------------------------------------------
export const SHIPS_PER_PLAYER = [5, 4, 4];
export const BOARD_WIDTH = 16;
export const BOARD_HEIGHT = 10;
export const COLUMNS = Array.from({ length: BOARD_WIDTH }, (_, i) => String.fromCharCode(65 + i));
